#include<sstream>
#include<iostream>
using namespace std;
string str;
int main()
{
      while(getline(cin,str))
      {
           stringstream stringStream(str);
           while(stringstream>>str)
           {
                 cout<<str<<endl;
           }
      }
      return 0;
}
